name: 🧠 Backend Deployment Listener

on:
  repository_dispatch:
    types: [deploy_triggered]

jobs:
  deploy-backend:
    runs-on: ubuntu-latest

    steps:
      - name: 🪄 Show who triggered this
        run: |
          echo "Evento recibido desde: ${{ github.event.client_payload.source }}"
          echo "Repositorio que lo envió: ${{ github.event.sender.login }}"
          echo "Branch: ${{ github.event.client_payload.branch || 'deploy' }}"

      - name: 🚀 Checkout backend repo
        uses: actions/checkout@v4

      - name: ⚙️ Run backend deployment
        run: |
          echo "Iniciando despliegue del backend..."
          # Aquí iría tu comando real de despliegue
          # Ejemplo:
          # ./deploy-backend.sh
